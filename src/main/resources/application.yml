# 1. Custom Reset Logic Configuration
password-reset:
  expiry-minutes: 30

# 2. Server Settings
server:
  port: 8081

# 3. Main Spring Configuration
spring:
  application:
    name: auth-service
  cloud:
    gateway:
      discovery:
        locator:
          lower-case-service-id: true
  # Database Configuration
  datasource:
    url:  jdbc:mysql://3utXacfbRoTt2ay.root:LbgcFSh3LDgvxXgY@gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY
    username: 3utXacfbRoTt2ay.root
    password: LbgcFSh3LDgvxXgY
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


  # Email Configuration (Gmail example)
  mail:
    host: smtp.gmail.com
    port: 587
    username: unevallalit6499@gmail.com  # Replace with your email
    password: mahy xyfx gkyu tnut      # Replace with Gmail App Password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

    # For testing, you can use MailHog (fake SMTP server)
    # host: localhost
    # port: 1025
    # username:
    # password:

# 4. Service Discovery (Eureka)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
#app:
#  frontend:
#    url: http://localhost:4200  # Your React/Angular frontend URL
#  backend:
#    url: http://localhost:8081  # This auth service URL