# ================================================================
# Auth Service - Application Configuration
# ================================================================

# 1. Custom Reset Logic Configuration
password-reset:
  expiry-minutes: 30

# 2. Server Settings
server:
  port: 8081

# 3. Main Spring Configuration
spring:
  application:
    name: auth-service

  # Database Configuration (Using TiDB Cloud)
  datasource:
    url: jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY
    username: 3utXacfbRoTt2ay.root
    password: LbgcFSh3LDgvxXgY
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA / Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false  # Disable to suppress warning in logs

# ================================================================
# 4. SendGrid HTTP API Configuration (Replaces spring.mail SMTP)
# ================================================================
sendgrid:
  api-key: ${SENDGRID_API_KEY}   # Set this in Render Environment Variables

# ================================================================
# 5. App URLs
# ================================================================
app:
  mail:
    from: unevallalit6499@gmail.com   # Must be verified in SendGrid Single Sender
  frontend:
    url: https://internship-project-frontend-sxg9.vercel.app/reset-password?token=
  backend:
    url: https://projectbackend-authservice.onrender.com

# ================================================================
# 6. Eureka Service Discovery
# ================================================================
eureka:
  client:
    service-url:
      defaultZone: https://projectbackend-serviceregistery-1.onrender.com/eureka/
  instance:
    prefer-ip-address: false

# ================================================================
# 7. Actuator
# ================================================================
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always