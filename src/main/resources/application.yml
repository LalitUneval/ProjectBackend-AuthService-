# 1. Custom Reset Logic Configuration
password-reset:
  expiry-minutes: 30

# 2. Server Settings
server:
  port: 8081

# 3. Main Spring Configuration
spring:
  application:
    name: auth-service
  
  # Database Configuration (Using TiDB Cloud)
  datasource:
    url: jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY
    username: 3utXacfbRoTt2ay.root
    password: LbgcFSh3LDgvxXgY
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.sendgrid.net
    port: 587
    username: apikey
    password: ${SENDGRID_API_KEY}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# 4. App URLs (Updated for Production)
app:
  mail:
    from: unevallalit6499@gmail.com
  frontend:
    # Update this to your actual deployed frontend URL when ready
    url: https://internship-project-frontend-sxg9.vercel.app/reset-password?token=
  backend:
    url: https://projectbackend-authservice.onrender.com

# 5. Service Discovery (Eureka) - UPDATED FOR LIVE LINK
eureka:
  client:
    service-url:
      # Use your new live Eureka URL here
      defaultZone: https://projectbackend-serviceregistery-1.onrender.com/eureka/
  instance:
    # In cloud environments like Render, prefer-ip-address should often be false 
    # so services register with their public domain name instead of internal IPs.
    prefer-ip-address: false